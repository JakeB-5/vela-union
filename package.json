{
  "name": "vela-union",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "4-system agent orchestration platform",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "lint": "eslint --quiet .",
    "typecheck": "tsc --noEmit",
    "vela": "node packages/vela-cli/dist/cli.js",
    "test:bootstrap": "node --experimental-strip-types scripts/test-bootstrap.ts"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "pnpm": {
    "overrides": {
      "@paperclipai/shared": "link:/Users/jin/projects/paperclip/packages/shared"
    }
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.2"
  }
}
